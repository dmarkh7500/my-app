import React from "react";
import './Checkbox.css'

const Checkbox = ({name}) =>
        <div>
            <input type="checkbox" />
                <label>{name}</label>
        </div>
    
export default Checkbox;

